{
  # Enable Docker
  virtualisation.docker.enable = true;
  
  # Optionally set storage driver
  #services.docker.storageDriver = "overlay2";

  # Optionally expose Docker API on a port
  #services.docker.tcpSocket = "tcp://0.0.0.0:2375";
}